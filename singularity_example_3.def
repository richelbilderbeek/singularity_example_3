BootStrap: library
From: richelbilderbeek/default/ubuntu_disco:ubuntu 

%runscript

    cowsay "cowsay works!"

%environment

    PATH=$PATH:/bin:/usr/games

%post

   apt-get update && apt-get install -y wget

    apt-get update
    apt-get install --yes cowsay
    apt-get install --yes locales
    locale-gen en_US.UTF-8

%labels

    AUTHOR Richel J.C. Bilderbeek

    NAME singularity_example_3
 
    DESCRIPTION Singularity example 3: Singularity 2.5, Ubuntu 19.4 and cowsay

    USAGE simply run the container

    URL https://github.com/richelbilderbeek/singularity_example_3

    VERSION 1.0
