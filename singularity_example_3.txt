Bootstrap: shub

From: pavel-demin/singularity-ubuntu:1804

%runscript

    echo "Rooooar!"

    echo "Arguments received: $*"

    echo "$@"

    # Don't use exec
    echo "Do not use exec"
    exec echo "Do not use exec"

%post

    apt-get update && apt-get -y install cowsay

    echo "The post section is where you can install, and configure your container."